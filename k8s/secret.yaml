# DO NOT commit real values — populate via:
# kubectl create secret generic txmonitor-secrets \
#   --from-literal=ALCHEMY_WS_URL='wss://...' \
#   --from-literal=COINMARKETCAP_API_KEY='...' \
#   -n txmonitor
apiVersion: v1
kind: Secret
metadata:
  name: txmonitor-secrets
  namespace: txmonitor
type: Opaque
stringData:
  ALCHEMY_WS_URL: "REPLACE_ME"
  COINMARKETCAP_API_KEY: "REPLACE_ME"
